# FlashDuty 同轮告警聚合分析：批准计划与执行交接

> 状态：计划已批准；实现尚未开始。本文是供新 Agent 直接执行的独立文档，包含批准计划和计划形成过程的压缩上下文。

## 执行交接

- 目标仓库：当前项目根目录。
- 执行要求：逐步实施下方 12 个 Approach 项；每步完成后先做对应的定向验证，再进入下一步；最后执行 Verification 中的聚焦测试和 UI smoke。
- 当前代码状态：未修改任何应用、迁移、测试或前端文件；仅创建了本文档。此前只读取了仓库和语言服务器状态，没有运行测试、构建、lint、迁移或服务。
- 必须先遵守 `analyze-database-alerts` skill：每个 FlashDuty 成员的 `/alert/info` 必须先于 knowledge、MCP 和因果判断；`alarm_host/alarm_port` 只取 `/alert/info`；仅现有单主 Agent 可判断因果；MCP 保持只读；无合格实时证据时严格返回 `现有结果无法得出根因`/`INCONCLUSIVE`。
- 多成员组只有 synthetic aggregate 是可见资源、run owner、调度 owner 和通知 owner；hidden raw members 没有自己的 run、结果或通知。单成员继续使用原 alert。
- 批次、成员、target、窗口、分组审计和 run subject 必须持久化；不能用 title、list item、instance 或兄弟成员补齐 host/port。

## 计划形成过程（compact）

### 1. 仓库事实映射

- 当前入口是 `app/application/scheduler.py::FlashDutyAlertPoller.poll_window`：完整分页后逐条 `AlertAnalysisService.ingest`，再逐条 `scheduler.enqueue(alert_id)`；没有 batch/group/member 身份。
- 当前权威详情由 `app/agents/nodes.py::enrich_alert_node` 在 run 建立后调用 `FlashDutyAlertDetailEnricher.enrich`；graph 顺序为 `enrich_alert -> fingerprint -> knowledge -> ReAct -> advise -> validate -> report`。
- `FlashDutyAlertSourceAdapter.normalize` 会清除 list item 中的 host/port，只有 `normalize_detail` 允许 `/alert/info` 的 `alarm_host/alarm_port` 进入 `DatabaseTarget`。
- 当前 persistence 以一个 `AlertRow` 为生命周期 owner，`InvestigationRunRow.alert_id`、progress、evidence、validation、checkpoint 和结果均围绕一个 alert；因此选用 synthetic owner 可以避免 shared-run fan-out。
- `InMemoryAnalysisScheduler.start`、`KafkaAnalysisScheduler.start` 会扫描 `RECEIVED/QUEUED/ANALYZING`；新 raw members 在成组前必须持久但不可见、不可调度。
- Generic MCP、Prometheus、Archery 和 FlashDuty tools 都存在从单 alert `occurred_at` 重算窗口的路径；必须统一改为 run-frozen subject envelope。
- 当前通知在 terminal finalize 后直接发送，没有 durable delivery 状态；为保证 terminal crash 后可恢复，批准计划增加 per-run notification outbox。

### 2. 产品决策闭合

- 候选边界固定为 `/alert/info` 的 `(alarm_host, alarm_port)`。
- 相同且非 `unknown` 的 canonical alert type 确定性合并。
- 同 endpoint 的不同已知类型由 AI 对“唯一 type ID”做完整互斥分区；模型不能拆开重复类型。
- 模型失败时每个唯一类型单独一组，重复类型仍合并；本轮不阻塞。
- 多成员只显示 synthetic aggregate；单成员不创建 wrapper。
- reanalysis 固定原 member IDs、ordinal、target 和窗口，不重分组、不拆组；后续 attempt 可重取成员 detail，但 endpoint 漂移必须在 knowledge/MCP 前 fail closed。
- 成员窗口均为 `[occurred_at-5m, occurred_at]`；融合窗口固定为连续包络 `[min(member.start), max(member.end)]`，成员间空档也纳入取证。
- detail 获取失败不得用 list/title 分组；该 raw alert 退化为 visible singleton，由 analysis detail gate 重试，不阻塞其它成员。

### 3. 设计收敛

- 强类型输入采用“一个 analysis owner + 必填 `InvestigationSubject`”，singleton/aggregate 共用同一 graph 和工具链。
- persistence 采用 batch row + `alerts` 自引用 owner/member 字段；raw FlashDuty records 继续承担去重和权威 detail provenance，避免引入第二套 raw alert 存储。
- synthetic source 固定为 `flashduty_aggregate`，防止其 external ID 被发送给 `/alert/info`。
- type partition 复用 react role model、reasoning effort 和 `AI_TIMEOUT_SECONDS`，不新增 `AI_GROUPING_*` 配置。
- grouping metadata 存 batch audit：endpoint、输入 type IDs、最终 type partition、展开后的 raw members、accepted/degraded、provider/model/prompt version/request ID/usage、failure type。
- 首次 run 复用 poll 阶段已取得的 details；reanalyze/后续无 checkpoint attempt 对冻结成员重取 details；run subject snapshot 在 detail gate 后冻结。
- FlashDuty detail 和 similar incidents 永久 context-only，不可成为 SUPPORTED 根因 evidence。

### 4. 最终 reviewer 修正

- 将 AI 输出字段从含糊的 member IDs 改为 `type_ids`，先验证唯一 canonical types 的 exact partition，再确定性展开到全部 raw members。
- 增加 `NotificationOutboxRow` 和 lease-safe dispatcher；terminal result 与唯一 outbox row 同事务提交，API/Worker startup 恢复 PENDING/过期 DELIVERING。由于 WeCom webhook 无幂等键，只能保证一个逻辑消息和至少一次投递，不能虚假承诺跨系统 exactly-once。
- 明确由 `DurableOuterToolDispatcher.execute` 将 `context.subject` 传入 `_project_tool_result` 和 `ToolResultAnalyzer.analyze`，任何恢复/重投影路径不得绕过 frozen window/target 校验。
- reviewer 对修正后的最终计划结论为 `NO_BLOCKING_GAPS`。

## 批准的实施计划

### Context

当前 `FlashDutyAlertPoller.poll_window` 在读完整轮 `/alert/list` 后仍逐条入库、逐条调度，权威 `/alert/info` 则要到单告警 run 的首个节点才读取，因此同一轮同实例告警无法共享一次分析。改造后只对本轮真正新建的原始告警做 detail-first 聚合：严格按 `/alert/info` 的 `(alarm_host, alarm_port)` 分桶，同类型确定性合并，不同类型只让模型做关系分区。多成员组创建一个唯一可见、可调度、可通知且拥有唯一 run 的 synthetic aggregate；成员、各自五分钟窗口和连续融合包络进入同一个现有主 Agent，单成员继续使用原告警。

### Approach

1. **建立唯一的强类型分析主体，同时保留单一生命周期 owner。** 在 `app/domain/models.py` 增加严格的 `InvestigationWindow(start, end)`、`InvestigationMember(alert, window, detail_request_id)` 和 `InvestigationSubject(contract_version="investigation-subject/v1", analysis_alert_id, kind, members, target, target_provenance, evidence_window, grouping_method, poll_batch_id, digest)`；`kind` 只允许 `singleton|aggregate`，`target_provenance` 只允许 `flashduty_alert_info|source_payload|none`。validator 必须保证成员非空且按 `(occurred_at UTC, external_id, local UUID)` 排序、ID/ordinal 唯一、每个成员窗口严格为 `[occurred_at-5m, occurred_at]`、融合窗口严格为 `[min(member.window.start), max(member.window.end)]`、aggregate 的全部成员 host/port 与 subject target 完全一致；`digest` 是排除 `raw_payload` 和自身字段后的 canonical JSON SHA-256。`AgentState` 与 `InvestigationContext` 保留现有 `alert` 作为唯一可见/run owner，但新增必填 `subject` 作为知识、主 Agent、工具和校验所用的事实输入；singleton 也构造一成员 subject，禁止 `None`/旧单告警旁路。扩展 `AlertRepository`、`KnowledgeSource`、`AIAdvisor`、`ConclusionValidator`、`ToolResultAnalyzer` 和 `AnalysisResultEvent` 的协议以显式传递 subject；其中工具执行形状仍为 `execute(request, context)`，run/progress/evidence/validation 的 owner 仍是一个 `alert_id`。

2. **用 migration `0017_flashduty_alert_aggregation.py` 持久化轮次、隐藏成员、owner、run 快照和通知 outbox。** 在 `app/adapters/persistence.py` 增加 `FlashDutyPollBatchRow`：`id`、UTC `window_start/window_end`、排序后的 `channel_ids/integration_ids` JSON、`status`（`COLLECTED|GROUPING|READY|PUBLISHED`）、`lease_owner/lease_expires_at/fencing_token`、严格校验后的 `detail_failures_json/grouping_decisions_json`、`last_error`、时间戳。给 `alerts` 增加 `alert_kind`（默认 `SOURCE`，另有 `FLASHDUTY_AGGREGATE`）、`is_analysis_resource`（历史行默认 true）、`poll_batch_id`、自引用 `analysis_owner_id ON DELETE CASCADE`、`member_order`、`poll_detail_status`（`PENDING|LOADED|FAILED`）、`aggregation_key`、`grouping_method`、`subject_json/subject_digest` 和 UTC `analysis_window_start/end`；增加 `(is_analysis_resource,status)`、`poll_batch_id`、`(analysis_owner_id,member_order)` 索引及 `(poll_batch_id,aggregation_key)` 唯一约束。新增隐藏状态 `AlertStatus.GROUPED`：多成员原始行设为 `GROUPED/is_analysis_resource=false/analysis_owner_id=<synthetic id>`；单成员原始行保持 owner 且转 `QUEUED/is_analysis_resource=true`。给 `investigation_runs` 增加可空的 `subject_snapshot_json/subject_digest` 以兼容 0017 前历史行；所有新 run 必填，历史行读取时由原 alert 确定性构造 singleton subject。新增 `NotificationOutboxRow`：确定性 UUID、`alert_id/run_id` cascade FK、`kind="analysis_result"`、`status=PENDING|DELIVERING|DELIVERED`、通知 lease/fence、`attempt_count/next_attempt_at/last_error/provider_delivery_id` 和时间戳，并对 `(run_id,kind)` 唯一，保证每个 aggregate run 只有一个逻辑通知。把 `DATABASE_SCHEMA_REVISION`、schema snapshot/assertions 和 Alembic head 更新到 `0017`。

3. **把一轮完整列表变成一个先落库、后取详情、再成组的可恢复批次。** 重写 `app/application/scheduler.py::FlashDutyAlertPoller.poll_window`：保留 `_fetch_complete_window` 的完整游标校验；随后一次 repository 事务按稳定 external ID 顺序创建本轮 batch，并对全部 list item 做 `source="flashduty"` 去重，新 raw 行先写 `RECEIVED/is_analysis_resource=false/poll_detail_status=PENDING`，整个事务内不 enqueue。list item 只要有一条不能 normalize/persist 就回滚整轮并让 poll 失败，禁止分析部分轮次；若全部 fetched items 都已存在，则不保留空 batch。只有 `created=True` 且首次归属该 batch 的 raw 行参加本轮分组；重叠窗口中已存在的告警只解析既有 owner 映射，不重新读 detail、不吸收到新组。扩展 `FlashDutyClient.alert_info(alert_id, *, retry_until_cancelled=True)`、`FlashDutyAlertDetailEnricher.enrich(..., retry_until_cancelled=True)`，poll 阶段传 `False`，沿用 `FLASHDUTY_MAX_RETRIES` 的有限重试；最多 8 个 `/alert/info` 并发，逐项在短 fenced transaction 中写回完整、sanitize 后的 detail 与 `request_id`，且 `alarm_host/alarm_port` 仍只能由 `normalize_detail` 写入。detail 成功的成员以 `normalize_detail` 的 `occurred_at` 冻结事实和窗口；detail 失败 singleton 才沿用已入库的 list occurrence，后续详情重试不得移动该事件窗口。单条 detail 请求失败不阻塞整轮：记录不含敏感报文的 `external_id/failure_type`，把该 raw alert 作为可见 singleton 入队，分析首节点继续用默认无限重试（最终受 `ANALYSIS_TIMEOUT_SECONDS` 限制）；禁止从 `/alert/list`、标题、instance 或兄弟成员补 target。detail 成功但 host 或合法 port 缺失的成员也只形成 singleton，并在 run 的 detail gate 处 fail closed。

4. **实现确定性的 endpoint/type 分桶和受限 AI 关系分区。** 在新文件 `app/domain/flashduty_aggregation.py` 放纯函数（仓库没有等价归组组件，避免把规则散落进 scheduler）：host key 仅做 Unicode NFKC、trim、casefold 和尾点去除，port 使用 `/alert/info` 已校验整数；类型 key 对 `NormalizedAlert.alert_type` 做 NFKC、trim、连续空白折叠和 casefold，空值或 `unknown` 永不与其它成员合并。每个有效 endpoint 桶先建立“ordered unique canonical type -> 该类型全部 raw members”的映射：同一非 unknown 类型不可拆分；只有至少两个不同已知类型时才调用一次模型，unknown 成员始终单独成组。在 `AIAdvisor` 增加 `partition_alert_types(*, alert_types: list[str]) -> tuple[AlertTypePartition, AdvisorMetadata]`，参数明确是按首次出现顺序去重后的 canonical type 列表；adapter 发送 `[{id, alert_type}]`，输出合同固定为 `{"contract_version":"alert-type-partition/v1","groups":[{"type_ids":[0,2]}]}`，Pydantic `extra="forbid"`，本地再拒绝 bool、负数、越界、重复、遗漏和空组，并验证 `type_ids` 是 `range(len(alert_types))` 的精确互斥分区。接受后先规范为组内升序、组间按最小 type ID 升序，再由确定性映射把每个 type ID 展开为该类型的全部 raw members；模型永远不能拆开重复类型，审计同时保存 type partition 和展开后的 member external IDs。`app/adapters/ai.py` 使用独立 inline prompt 和 literal `flashduty-alert-type-partition-v1`，继续采用“schema 入 prompt + JSON object mode + Pydantic 校验”，复用 `ai_react_model/ai_react_reasoning_effort`；prompt 明令不输出根因、处置或 host 推断。`AlertAnalysisService.partition_alert_types` 捕获当前 advisor，在整个 transport retry/JSON repair 外包一层 `asyncio.timeout(AI_TIMEOUT_SECONDS)`；把 `_active_analyses` 泛化为 active adapter users，使运行时热替换不会关闭正在分组的旧 client。endpoint 分区调用最多 4 个并发且彼此隔离；`FakeAIAdvisor`/`ConservativeFallbackAdvisor` 返回每个唯一 type ID 一组。每个 endpoint 决策审计固定保存 endpoint、输入 ID/type、最终 type partition、展开后的 raw members、`outcome=accepted|degraded`、provider/model/prompt_version/request_id/accepted-response usage，以及失败时仅保存 `failure_type`；不把 exception message 或密钥落库。超时、provider 错误或非法 exact partition 时，本地降级为“每个唯一类型一组”，因此重复类型仍合并，不同类型永不误合并，本轮继续。

5. **确定性构造 synthetic owner，并在一个事务中冻结成员关系。** 每组成员按既定稳定顺序排列；`aggregation_key` 为 version `flashduty-group/v1`、canonical endpoint 和成员 external IDs 的 canonical JSON SHA-256，group UUID 用 batch UUID 作 namespace 对该 key 做 UUID5。成员数为一时直接提升原 raw row 为 owner，不创建 wrapper；成员数大于一时创建 `id=<group UUID>`、`external_id="aggregate-<group UUID>"`、`source="flashduty_aggregate"`、`alert_kind=FLASHDUTY_AGGREGATE` 的 synthetic `NormalizedAlert`，避免任何代码把 synthetic ID 发往 `/alert/info`。synthetic 字段固定为：最高严重度（`CRITICAL>WARNING>INFO`）、`occurred_at=max(member.occurred_at)`、IPv6 安全格式化后的 `title="<host:port> 聚合告警（N 条）"`、`reason="相关告警类型：<按首次出现去重的类型，以、连接>"`、`description="同一轮 FlashDuty 轮询中由 N 条相关告警组成；详情列出全部成员与冻结时间窗口。"`、`incident_fingerprint="aggregate-v1-<aggregation key 前32位>"`；host/port 固定为共同权威 endpoint，engine/instance/database 及 environment/service/resource/cluster 等字段只有全体非空且完全一致时保留，否则可空字段置 `None`、必填字符串置 `"multiple"`（全体为空则 `"unknown"`）；`labels={}`、`raw_payload={}`，只在 features/attributes 保存成员数、类型、batch/group ID 和窗口，不复制成员正文。repository 的 `finalize_flashduty_batch` 在 batch lease/fence 下验证每个新 raw 恰好出现一次，原子创建 synthetic owner、更新 hidden members、保存 owner subject/digest/window、写入严格分组审计并把 batch 置 `READY`；任一不变量失败整笔回滚。

6. **保持一次发布、一次 run、一个逻辑通知，并补齐崩溃恢复。** grouping commit 后才在事务外为每个 analysis owner enqueue 一次；全部成功后把 batch 置 `PUBLISHED`，部分/全部 publish 失败时保持 `READY`，下次恢复重发整批 owner，现有 in-memory 去重、Kafka 至少一次语义和 DB run lease 吸收重复。当前 `poll_window` 的 publish 有任何失败时仍向上抛错（管理员接口保持 502、后台 loop 记录失败），但绝不回滚已冻结 group；恢复后可安全补发。`FlashDutyAlertPoller.start` 无论是否开启周期轮询都先重发 `READY` batch；client 可用时再各尝试一次 `COLLECTED` 或 lease 过期 `GROUPING` batch，续取仍为 PENDING 的 details 并完成 grouping，之后才启动周期 loop。batch claim/renew/release 复用现有 analysis lease 秒数、heartbeat 周期和 fencing 规则；`poll_window` 开始时也尝试同样的 pending snapshot，但一个 invocation 不无限重试同一坏 batch。`InMemoryAnalysisScheduler.start`、`KafkaAnalysisScheduler.start` 和 repository `list_by_status` 只扫描 `is_analysis_resource=true` 的 `RECEIVED/QUEUED/ANALYZING` owner；hidden RECEIVED/GROUPED rows 永不发布。把 COMPLETED/INCONCLUSIVE run 的唯一 outbox row 与 terminal recommendation、validation 和状态放进同一个 `finalize_run` fenced transaction；`AlertAnalysisService.start`（由 API lifespan 和 `KafkaAlertWorker.run` 在 repository initialize 后调用）启动一个多进程 lease-safe dispatcher，启动时先 drain PENDING/过期 DELIVERING，之后由 finalize event 唤醒并按 5 秒起、最多 5 分钟的指数退避无限重试。dispatcher 按 outbox 的 run ID 加载该 run 的冻结 subject/result，调用当前 notifier；成功后以 outbox fence 原子写 `DELIVERED/provider_delivery_id` 和 REPORTING progress，失败只写 sanitized error、下一次时间并释放为 PENDING，不再依赖已终态 run lease。retention 只选择到期 terminal owner 且要求其 outbox 已 DELIVERED；删除 synthetic owner 时由 FK cascade 删除 members/runs/evidence/outbox，随后删除无 alerts 引用且到期的 PUBLISHED batch，禁止单独清理 GROUPED member。

7. **让每个 run 在 knowledge 前具备完整成员 `/alert/info`，但永不重组。** `AlertAnalysisService._analyze_claimed_alert` 按 owner 加载 owner subject 并写入 run snapshot；首次 run 对 poll 阶段已 `LOADED` 的成员直接物化 detail，不重复网络调用。`app/agents/nodes.py::enrich_alert_node` 改为对 subject 的每个真实 FlashDuty member 生成稳定的 run-scoped detail evidence（`uuid5(run.id, "flashduty-alert-info-evidence-v2:<member external_id>")`、`tool_name="flashduty_alert_info"`、`source_system="flashduty_alert_detail"`），保存完整 sanitize 后的 `/alert/info`、request ID、member ID、member window 和权威 target；全部成功并保存 run subject snapshot 后才进入 fingerprint/knowledge。poll detail 失败后降级的 singleton 若在首次 run 成功补到合法 target，repository 只允许在原 owner target 为空时一次性冻结该 target 和更新 owner subject；之后与普通 singleton 一样不可漂移。显式 reanalysis 或无 checkpoint 的后续 attempt 对冻结 external IDs 逐成员重新调用 `/alert/info`，刷新当前 run 的成员事实但不改变 member set、ordinal、原 occurred_at、每成员窗口、融合窗口或原 group target；任一 detail 缺失或新 host/port 与冻结 endpoint 不同即在 knowledge/MCP 前把该 run 置 FAILED，不删成员、不拆组、不重新分区。checkpoint reclaim 读取同一 run subject snapshot，不重复已完成的 detail node。原始 hidden members没有 run/status/recommendation；aggregate owner 只有一个 progress/evidence/validation/result 流，reanalyze/cancel/trace 继续只接受 owner ID。

8. **让知识和唯一主 Agent消费所有成员事实，而不是 synthetic 摘要。** 将 `KnowledgeSource.search`、`KnowledgeSourceRegistry.search`、`ExternalKnowledgeSource.search`/`build_search_query` 切换为必填 subject；每个启用的 knowledge source 对整组只调用一次，查询按稳定顺序汇总并去重所有成员的 engine、alert type、metric、error pattern/summary、resource、service、environment 和 reason，继续保留现有 source timeout、failure isolation、score threshold 与 `(source, knowledge_id)` 去重。`react_decide_node`、`advise_node`、`OpenAICompatibleAdvisor`/Responses/Fake/Fallback 的 payload 同时明确标注 display-only `analysis_alert` 和 causal-context `subject`，包含每个成员的 normalize 后事实（不重复 raw payload）、每成员窗口、连续融合窗口、共同权威 target、全部 detail context、knowledge 和实时 MCP evidence；仍只执行现有一个主 Agent ReAct。关系分区模型不是根因 Agent，不能提出或持久化因果结论。aggregate fingerprint 使用已冻结的 `aggregate-v1` 值；singleton 沿用现有 fingerprint 逻辑。

9. **把融合窗口和共同 target 从 subject 传遍所有工具，删除各自重算。** `execute_react_tool_node` 构造的 `InvestigationContext` 必填 subject；`ToolResultAnalyzer.analyze` 增加必填 `subject`。在 `app/agent_runtime/outer_dispatch.py` 中由 `DurableOuterToolDispatcher.execute` 把 `context.subject` 显式传给 `_project_tool_result`，再传给 analyzer；冲突解析/terminal replay 只复用已持久化且 input identity 绑定同一 run/subject digest 的 projection，任何重新投影路径也必须带同一 subject，不能因缓存或恢复绕过 window/target 校验。`GenericMCPEvidenceTool` payload 发送 analysis owner、全部 members/member windows、共同 target 和 exact `evidence_window`。Prometheus 的 client/harness/messages/range projection/tool result 全改接显式 start/end；`required_window.duration_seconds` 使用真实包络秒数，`DeterministicToolResultProcessor._prometheus_required_window` 删除 `duration==300`，改为严格等于 subject window，并继续拒绝窗外样本或 target 不匹配。Archery 的 `execute_slow_log_query`、harness state/messages/history SQL 接受显式 start/end，删除 `client.window_seconds` 以及已失去语义的 `ARCHERY_SLOW_LOG_WINDOW_SECONDS` Settings/factory/env/test 引用。所有这些 provider 只执行一个连续包络查询，空档不裁掉。

10. **按 subject 改造 FlashDuty 只读上下文工具，禁止代表成员和 synthetic ID 泄漏。** `FlashDutyAlertContextTool` 在 aggregate 未指定 `member_external_id` 时只返回全部冻结 member local facts；指定时必须校验该 ID 属于 subject，再只查询该真实 member 的 info/events/feed，永不 fan-out 全组也永不请求 synthetic external ID。`query_similar_incidents` 对 aggregate 必须显式给出且属于某成员的 incident ID。`FlashDutyChangesTool` 使用融合窗口，默认 query 仅在所有成员 service 一致时采用；metrics/logs 的 time range 固定为融合窗口，默认 metric 仅在成员存在唯一安全 metric 时采用，否则要求模型显式参数；database diagnostics 只使用 subject 的共同权威 target。`EvidenceRecord.is_context_only` 和投影路由把 `flashduty_alert_detail`（不只 similar-incidents）永久标为 context-only；`RuleConclusionValidator`、`enforce_post_evidence_root_cause_policy` 对比全部 member reasons 并排除 synthetic reason，保证只有 detail/历史相似信息时严格返回 `现有结果无法得出根因`/`INCONCLUSIVE`。

11. **让 API 明确区分 raw、analysis resource 和 job，同时只公开 owner。** `app/adapters/persistence.py::list_alerts/dashboard_summary/get`、搜索和 source filter 只公开 `is_analysis_resource=true`；`source=flashduty` 同时匹配 singleton `flashduty` 和 synthetic `flashduty_aggregate`，aggregate 搜索 haystack 通过一次批量 member 查询包含隐藏成员字段，避免 N+1。`AlertSummary` 增加 `member_count`、`is_aggregate`；`StoredAlert` 增加所选 run（无 run 时用 owner）对应的强类型 `subject`，隐藏 raw member ID 作为普通 `GET /alerts/{id}` 返回 404。将 `FlashDutyPollItemResult.stored` 改为 raw normalized alert、本地 raw ID、resolved owner ID/URL 和 created/grouped 标记，避免通过公开 `StoredAlert` 读取 hidden member。`app/api/schemas.py` 保留 `total_count/new_count/deduplicated_count` 为本次 raw `/alert/list` 计数，新增仅统计本次新 batch 的 `analysis_resource_count`、成功 publish 的 `enqueued_analysis_count` 和 `groups`；startup/旧 READY batch 的恢复发布不混入该响应。每个 raw item 新增 `local_alert_id/analysis_alert_id/analysis_detail_url/grouped`，每个 group 返回 owner ID、member external IDs、member count、权威 host/port、类型、member windows、融合 window 和 grouping method。`GET detail`、trace、cancel、reanalyze 与企微 URL 均继续使用一个 owner alert ID，无新增 group route 或 member redirect。

12. **把聚合语义投影到现有列表、详情与企微表面。** 在 `frontend/src/types/api.ts` 增加内部但不会公开到列表的 `AlertStatus.GROUPED`、`DatabaseTarget.port`、subject/member/window 类型、list 的 `member_count/is_aggregate` 和 `StoredAlert.subject`，并在 `frontend/src/lib/format.ts::statusLabel` 补只用于类型穷尽的 `GROUPED` 文案；`AlertTable.tsx` 仍一组一行，只在 aggregate 标题下显示“聚合 N 条”。`AlertDetailPage.tsx` 在 incident facts 后增加“聚合成员”卡：显示权威 `host:port`、融合窗口，并按 ordinal 展示每个成员 external ID、title、severity、alert type、occurred_at、五分钟窗口、reason/description；成员不可点击为独立详情，重分析文案固定为“按原成员与窗口重新获取详情并使用当前配置分析”。`WeComAlertViewPage.tsx` 同样显示成员数、共同 target 和融合窗口，并在 `frontend/src/styles.css` 复用现有 card/token 样式补齐响应式成员列表。`AnalysisResultEvent` 携带 run subject 和确定性 outbox notification ID，`build_wecom_template_card` 的 aggregate 横向字段固定为六项“告警级别、告警主机 host:port、原始告警 N 条、融合窗口、数据库、环境/服务”，不在现有六项后继续追加；singleton 维持原字段但告警主机同样只格式化 subject host+port，删除当前 `database.host or database.instance` fallback。每个 owner run 只创建并消费一条逻辑 outbox，不为 hidden members fan-out 通知。

### Critical files & anchors

- `app/application/scheduler.py::FlashDutyAlertPoller.poll_window/start` — 完整批次入库、detail-first、分区、恢复和每 owner 一次发布的总编排。
- `app/adapters/persistence.py::{AlertRow,InvestigationRunRow,SQLAlchemyAlertRepository}` — hidden member/self-owner、batch lease、subject snapshot、owner-only 查询和 cascade retention。
- `migrations/versions/0017_flashduty_alert_aggregation.py` — 0017 表、列、约束、索引与历史默认值。
- `app/domain/models.py::{InvestigationSubject,NormalizedAlert,StoredAlert,AlertSummary}` — 单 owner 与多成员事实、窗口和公开投影的强类型合同。
- `app/agents/nodes.py::{enrich_alert_node,knowledge_match_node,react_decide_node,execute_react_tool_node,advise_node,validate_node}` — detail-first 后唯一知识/Agent/MCP/校验链。

### Verification

- 聚合/AI 单元合同：在 `tests/unit/test_ai.py` 覆盖 Chat 与 Responses provider 的专属 prompt/model/effort、完整合法分区及遗漏/重复/bool/负数/越界/坏 JSON/超时降级；在 `tests/unit/test_scheduler.py` 覆盖“先穷尽 list 分页、再为每个 new raw 调 detail、再 grouping、最后 enqueue”的事件顺序，相同类型不调用模型、不同端口绝不合并、unknown/缺 target/detail 失败单独分析、模型失败只保留同类型合并、重叠轮不重取 detail/不重复建组，以及 COLLECTED/READY 崩溃恢复。
- 持久化/生命周期合同：在 `tests/unit/test_persistence_schema.py`、`test_persistence_harness.py`、`test_alert_retention.py` 验证 0016→0017 migration、历史行仍可见、batch fenced finalize 全回滚、唯一成员归属、owner-only startup scan、synthetic 删除级联 members/runs/evidence/outbox/batch，以及 pending outbox 阻止 retention；在 `test_service_run_lease.py` 与 `test_langgraph_checkpoint.py` 验证同一 aggregate 只有一个 leased run，checkpoint 与 run subject digest 不变。
- 分析正确性合同：在 `tests/unit/test_workflow.py` 用两个成员断言两条 `/alert/info` evidence 全部先于 knowledge/MCP，主 Agent payload 同时包含成员事实、两个五分钟窗口和融合包络；重分析固定 IDs/ordinal/窗口且重新取 detail，endpoint 漂移在 knowledge 前 FAILED；仅 member detail evidence 必须为 `INCONCLUSIVE`。在 `test_generic_mcp.py`、`test_prometheus_mcp.py`、`test_prometheus_harness.py`、`test_tool_result_analysis.py`、`test_archery_mcp.py` 覆盖 exact envelope/target 传递、范围不匹配拒绝和包络大于 300 秒。
- API/通知合同：在 `tests/integration/test_api.py` 的手工 poll 场景返回 3 个新 raw，并用 scripted type partition `[[type0,type1],[type2]]` 形成 2 个 analysis resources/2 个 jobs；断言列表只有两个 owner、聚合详情包含两成员、raw member GET 404、poll raw/resource/job 三类计数与 owner 映射一致。`tests/integration/test_admin_api.py` 验证 dashboard/source/search 只统计 owner、reanalyze 复用原组；`tests/unit/test_notification.py` 验证每个 aggregate run 只有一条 outbox/卡片且 host+port/成员数/融合窗口正确，并模拟 finalize 后进程退出、startup drain 补发，以及多 dispatcher claim 不重复发送同一已 DELIVERED row。
- 运行聚焦测试：`python -m pytest tests/unit/test_ai.py tests/unit/test_scheduler.py tests/unit/test_workflow.py tests/unit/test_persistence_schema.py tests/unit/test_persistence_harness.py tests/unit/test_alert_retention.py tests/unit/test_service_run_lease.py tests/unit/test_langgraph_checkpoint.py tests/unit/test_generic_mcp.py tests/unit/test_prometheus_mcp.py tests/unit/test_prometheus_harness.py tests/unit/test_tool_result_analysis.py tests/unit/test_archery_mcp.py tests/unit/test_notification.py tests/integration/test_api.py tests/integration/test_admin_api.py`；随后运行 `npm --prefix frontend test` 与 `npm --prefix frontend run build`。
- 端到端 smoke：用 scripted FlashDuty client 返回同一 `(host,port)` 的三条新告警 A/B/C，scripted partition 把 A+B 相关、C 单独；调用管理员 poll 后观测 `/alert/info` 三次均早于两个 queue job，`GET /api/v1/alerts` 只新增两个 owner，逐个执行 job 后 aggregate 只有一个 run/一次通知且 Agent/MCP 看到 `[min(occurred_at)-5m,max(occurred_at)]`。启动 API 与 Vite，用 Browser 对列表和 aggregate detail 做实际页面检查；若本地没有真实 FlashDuty 数据，使用浏览器 request interception 返回上述已由 integration test 验证的 API fixture，确认一行“聚合 2 条”、成员顺序、host:port、成员窗口/融合窗口和冻结重分析文案均可见，控制台无错误。

### Assumptions & contingencies

- “同一轮”只指一次 `_fetch_complete_window` 完整分页后、该事务中 `created=True` 的 raw alerts；历史或重叠轮 deduplicated alerts 不参与本轮新组。
- 候选边界只能来自每条 `/alert/info` 的 `alarm_host + alarm_port`；host 仅做确定性规范化，任一字段缺失/非法或 detail 请求失败都退化为可见 singleton，绝不猜测或阻塞其它成员。
- 完全相同且非 `unknown` 的 canonical type 始终确定性合并；模型只分区同一 endpoint 内不同已知 type ID。模型失败时每个唯一 type 独立，重复 type 仍共享一次分析。
- 多成员只显示 synthetic aggregate；原 raw rows 仅是详情成员且没有独立 run/result/status/通知。单成员保留原 alert，不创建 synthetic wrapper。
- 每个成员窗口固定为事件前五分钟，融合取连续包络 `[min(occurred_at)-5m,max(occurred_at)]`；成员之间的空档也进入查询，不拆成多段。
- 首次 run 复用本轮刚取得的权威 details；reanalyze/后续无 checkpoint attempt 对冻结 member IDs 重取 details，但不重新分组或改变原 target/成员/窗口，target 漂移或 detail 不可用时 fail closed。
- type 分区复用现有 react role model、reasoning effort 和 `AI_TIMEOUT_SECONDS`，不增加 `AI_GROUPING_*` 配置；MCP 保持只读，因果判断仍只有现有单主 Agent。
- 通知 outbox 提供每个 run 唯一逻辑消息和至少一次恢复投递；WeCom webhook 没有可用的幂等键，因此进程若在远端已接受、但本地写 `DELIVERED` 前崩溃，恢复重试仍可能产生同 notification ID 的物理重复，不能虚假承诺跨系统 exactly-once。
- FlashDuty detail 和历史相似记录永久只是上下文；没有合格的实时因果证据时，聚合不会因成员更多而提升结论，必须返回 `现有结果无法得出根因`/`INCONCLUSIVE`。
